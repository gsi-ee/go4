# This is example of login script, required for compilation 
# of complete Go4 and user analysis under CYGWIN environment
#
# First ensure that all software components (MS VC, SDK, IDE)
# installed in directories without spaces in their names.
# If not, one should create symbolik links to such directories,
# using mklink utility under admin account. Do something like
#  > mklink /D c:\soft\vs "C:\Program Files\Visual studio" 
# Than adjust windows paths

export VC_PATH="E:\Soft\VS\VC"
export SDK_PATH="E:\Soft\SDK"
export IDE_PATH="E:\Soft\VS\Common7\IDE"

ROOT_PATH="F:\Soft\root"
GO4_PATH="F:\Soft\go4"
QT_PATH="E:\Soft\Qt451"

export GO4_OS=Win32
# export GO4SYS=`cygpath $GO4_PATH`

export ROOTSYS=`cygpath $ROOT_PATH`
export QTDIR=`cygpath $QT_PATH`
export LD_LIBRARY_PATH=$ROOTSYS/lib:$GO4SYS/lib:$QTDIR/lib:$LD_LIBRARY_PATH
export PATH=$ROOTSYS/bin:$GO4SYS/bin:$QTDIR/bin:$PATH
export INCLUDE="$VC_PATH\include;$SDK_PATH\Include;$QT_PATH\include"

export LIB="$ROOT_PATH\lib;$GO4_PATH\lib;$VC_PATH\lib;$SDK_PATH\Lib;$QT_PATH\lib"
export PATH=`cygpath $VC_PATH`/bin:$PATH

#this is required by MS cl compiler to find some dlls
export PATH=`cygpath $IDE_PATH`:$PATH

export PATH=`cygpath $SDK_PATH/bin`:$PATH

export QMAKESPEC=win32-msvc2008
